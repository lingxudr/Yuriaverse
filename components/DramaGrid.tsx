import Link from 'next/link';
import { SafeImage } from './SafeImage';
import type { DramaItem } from '../services/sansekai/types';
export function DramaGrid({items}:{items:DramaItem[]}){if(!items.length)return <div className="hub-empty big"><div>📭</div><b>Konten belum tersedia</b><p>Drama belum tersedia dari provider saat ini.</p></div>; return <div className="hub-grid-list">{items.map(it=><article className="hub-card" key={`${it.provider}-${it.id}`}><Link href={`/drama/${it.provider}/${encodeURIComponent(it.id)}`} className="hub-poster"><SafeImage src={it.poster} alt={it.title} fill sizes="190px" fallbackText={it.title}/><span className="hub-badge">{it.provider}</span></Link><div className="hub-card-body"><h3>{it.title}</h3><p>{it.episodeCount?`${it.episodeCount} Episode`:it.status||'Drama'}</p><div className="hub-actions"><Link className="btn" href={`/drama/${it.provider}/${encodeURIComponent(it.id)}`}>Tonton</Link><Link className="btn secondary" href={`/drama/${it.provider}/${encodeURIComponent(it.id)}`}>Detail</Link></div></div></article>)}</div>}
