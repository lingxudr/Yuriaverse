import { NextResponse } from 'next/server';
export const runtime='nodejs'; export const dynamic='force-dynamic';
export async function POST(req:Request){try{const form=await req.formData();const file=form.get('image');if(!(file instanceof File))return NextResponse.json({ok:false,message:'Upload gambar wajib'}, {status:400}); if(file.size>5*1024*1024)return NextResponse.json({ok:false,message:'Maksimal 5MB'}, {status:400}); const res=await fetch('https://api.trace.moe/search?anilistInfo&cutBorders',{method:'POST',headers:{'content-type':file.type||'image/jpeg'},body:Buffer.from(await file.arrayBuffer())}); const data=await res.json(); return NextResponse.json({ok:res.ok,data});}catch(e){return NextResponse.json({ok:false,message:e instanceof Error?e.message:'Trace search gagal'}, {status:500})}}
